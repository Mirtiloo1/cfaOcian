## Para inicializar:

1. cd backend-ml -> pip install fastapi uvicorn pandas scikit-learn -> uvicorn main:app --reload
2. cd backend-node -> npm install -> node server.js
3. cd cfaocian -> npm install -> npx expo start
